import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Colors
  static const Color primary = Color(0xFF9ECBE5); // main light blue
  static const Color secondary = Color(0xFF676666); // greyish accent

  // Backgrounds
  static const Color background = Colors.white;
  static const Color scaffold = Colors.white;

  // Text Colors
  static const Color textDark = Color(0xFF212121);
  static const Color textLight = Colors.white;
  static const Color textLightGrey = Color.fromRGBO(189, 189, 189, 1);
  static const Color textGrey = Color(0xFF676666);
  static const Color borderLightGrey = Color(0xFFB8B6B6);

  // Buttons
  static const Color buttonBackground = Color(0xFF9ECBE5);
  static const Color buttonText = Colors.white;

  // Shadows / Misc
  static const Color shadow = Color(0x29000000); // translucent black shadow
}
